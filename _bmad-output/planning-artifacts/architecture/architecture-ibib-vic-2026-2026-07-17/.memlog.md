---
scope: Toan he thong demo Compliance Copilot (Epic 1-7), 48h
purpose: build-substrate
altitude: feature
updated: 2026-07-17T18:33
---

- (decision) AD-1 Single source of truth: 1 JSON corpus {documents,clauses,edges} hand-annotated; BE graph + FE viz cung load, khong ai tu che schema
- (decision) AD-2 KnowledgeBase immutable, load-once luc startup (NetworkX + BM25 + vectors); request khong mutate; ingest = reload
- (decision) AD-3 Query = pipeline stage co dinh: retrieve->expand->temporal_filter->conflict_check->synthesize; RAG baseline = tat expand+temporal_filter bang flag
- (decision) AD-4 Clause la don vi nguyen tu + neo trich dan; moi thu key theo clause_id on dinh
- (decision) AD-5 Hieu luc = [effective_date, expiry_date) nua mo; 1 helper is_active(clause,asOf) duy nhat; default asOf=hom nay
- (constraint) AD-6 API contract dong bang API_CONTRACT.md: POST /api/chat {question}->{answer,sources}; them field phai additive; loi dung detail
- (decision) AD-7 Huong phu thuoc: api->pipeline->knowledgebase; LLM+embedding sau provider interface
- (constraint) AD-8 Secret+LLM chi o backend; FE khong giu key, chi goi /api/*
- (decision) AD-9 Determinism boundary: conflict/radar tinh tu graph runtime; canned fallback tach rieng, khong tron vao pipeline
- (event) Cai thien 5 so do mermaid: dependency (subgraph 4 tang), container (port+mode), data-flow (stage I/O + baseline flag), ERD (PK/FK), version-chain vi du
- (event) Reviewer gate: lint(1 low false-pos) + 3 subagent (rubric/versions/adversarial); 4 critical + high
- (decision) MVP BM25-only, bo embedding (e5/bge khong phai API san); dense = Deferred/[Day du]
- (decision) AD-10 fusion chuan hoa [0,1], AD-11 loc visibility tai retrieve
- (change) AD-6 mo rong: request{question,asOf,mode}, sources them clause_id; cap nhat API_CONTRACT.md that
- (change) Siet AD-1(schema commit,edges=clause_id) AD-2(atomic swap+pin) AD-3(conflict sau temporal) AD-5(null) AD-7(vectorstore iface); them muc Deployment; pin versions 7/2026
- (event) spine finalized
- (change) UPDATE: doi in-memory -> PostgreSQL. corpus.json = seed, Postgres = kho runtime
- (change) AD-1 amend: corpus.json la SEED versioned; Postgres la nguon su that runtime, seed tu corpus
- (change) AD-2 amend (dao nguoc): Postgres ben vung, query per-request; bo in-memory KB + atomic swap; ingest = INSERT ben vung
- (change) AD-3 amend: stages dieu phoi repository; temporal+conflict la SQL trong repository
- (change) AD-5 amend: is_active = menh de WHERE trong repository SQL
- (change) AD-7 amend (dao nguoc): them tang repository; api->pipeline->repository->Postgres; DB duoc phep
- (change) AD-10 amend: ranking = Postgres ts_rank chuan hoa [0,1] (thay BM25 lib)
- (change) AD-11 amend: loc visibility = WHERE trong repository.search
- (decision) AD-12 moi: Repository la ranh gioi truy cap DB duy nhat (seam); pipeline/api khong viet SQL; Clause dataclass = hop dong
- (decision) Stack: them PostgreSQL 17 + psycopg; bo NetworkX + rank_bm25 (dung Postgres FTS)
- (constraint) Database do nguoi khac (Epic 0) phu trach; ban giao qua repository.py
- (event) Spine updated to Postgres; lint pass; AD-1,2,3,5,7,10,11 amended + AD-12 added
