# Runtime
fastapi
uvicorn[standard]
pydantic>=2
httpx            # AnthropicLLM/OpenAI-compat gọi API
python-dotenv    # nạp backend/.env (LLM_API_KEY, DATABASE_URL)
python-multipart # upload PDF (multipart/form-data)

# Database (base DB-backed RAG — PostgreSQL/pgvector qua DATABASE_URL)
sqlmodel
sqlalchemy
psycopg2-binary  # driver postgresql:// cho SQLAlchemy
pgvector         # cột Vector(768) trong dwh.anh_xa
rank-bm25        # retrieve BM25 trong KnowledgeBase
networkx         # đồ thị quan hệ điều khoản

# Ingest tài liệu
pdfplumber       # bóc tách PDF (admin ingest)
pypdf            # cắt Điều cho tài liệu phiên (Story 7.6)
python-docx      # bóc tách .docx
pypdfium2        # render trang PDF → ảnh cho OCR
pytesseract      # OCR fallback (PDF scan/font hỏng). CẦN: tesseract + gói 'vie'
# OCR cần binary hệ thống: `brew install tesseract` + gói tiếng Việt vie.traineddata
# (tessdata_fast) đặt trong thư mục tessdata của tesseract.

# Test
pytest
