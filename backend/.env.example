# Key LLM chỉ ở backend (AD-8) — không commit .env thật
LLM_API_KEY=
LLM_BACKUP_API_KEY=

# Đường dẫn corpus (mặc định: ../data/sample/corpus.json)
# CORPUS_PATH=

# Địa chỉ frontend cho CORS (mặc định http://localhost:5173)
# FRONTEND_ORIGIN=http://localhost:5173

# DATABASE_URL (Epic 0 — chưa dùng ở Story 1.1)
# DATABASE_URL=postgresql://ibib:ibib@localhost:5432/compliance
